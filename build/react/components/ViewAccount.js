const React = require('react');

const ViewAccount = React.createClass({

    render: function() {
        return (
            <section>
                <p>View Account</p>
            </section>
        )
    }
});

module.exports = ViewAccount;
