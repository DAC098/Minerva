const React = require('react');

const Header = React.createClass({

    render: function() {
        return (
            <header>
                <p>header</p>
            </header>
        )
    }
});

module.exports = Header;
